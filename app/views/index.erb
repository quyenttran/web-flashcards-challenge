<div>
  <% if !logged_in? %>
    <h2> Sign Up or Log In to Play Flashcards! </h2>
  <% end %>

  <ul class=container container-table>
    <% @decks.each do |deck| %>
      <li><a href="/decks/<%=deck.id%>/preview"><%= deck.name.capitalize %></a> </li>
    <% end %>
  </ul>
</div>
